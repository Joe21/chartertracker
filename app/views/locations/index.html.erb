<h1 class="title">Locations Menu</h1>

<p>
	<em>
		Locations are based on ports of departure for charter fishing trips.<br />
		Captains need not worry, CharterTracker will <span class="bold-me">NEVER</span> track your coordinates and will <span class="bold-me">NEVER</span> steal your spots!<br />
	</em>
</p>
<br />

<h3>My Locations</h3>
<p>CharterTracker is tracking your trips for the following locations...</p><br>

<% @my_locations.each do |location| %>
	<div class="my-locations">
		<div class="five columns">• <%= location.name %></div>
		<div class="remove-button-container">
			<%= button_to "Remove", remove_from_my_location_path(location.id), :method => :post %>
		</div><br />
	</div>
<% end %><br />

<hr>
<h3>New Locations</h3>

Submit a new location for admin approval
	<div class="button-container">
		<%= button_to "New", new_location_path, method: 'get' %>
	</div><br />

<% if @approved_locations.count != 0 %>
	<div>Or add the following approved location(s) to your account
		<ul>
			<% @approved_locations.each do |location| %>
			<li>• <%= location.name %>
				<div class="button-container">
					<%= button_to "Add", add_to_my_location_path(location.id), :method => :post %>
				</div>
			</li>
			<% end %>
		</ul>
	</div>
<% end %>